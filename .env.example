# Copie para .env apenas se precisar sobrescrever os padrões locais.
# Não grave credenciais reais neste repositório.

# Caminho padrão: data/database/radar.sqlite3
RADAR_DATABASE_URL=sqlite:///data/database/radar.sqlite3

# Caminho padrão: config
RADAR_CONFIG_DIR=config
